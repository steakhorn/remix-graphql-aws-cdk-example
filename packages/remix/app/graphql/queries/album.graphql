query Album($albumId: String!) {
  album(albumId: $albumId) {
    id
    title
    releaseDate
    artist {
      name
    }
    tracks {
      id
      trackNumber
      title
      duration
    }
  }
}
